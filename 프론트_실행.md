# 브라보 챗 FE — 실행 가이드

Next.js 16 (App Router) · React 19 · Tailwind v4 · TypeScript

> 최초 1회: `npm install`

---

## 0. 환경 소개

두 가지 모드로 실행할 수 있고, `.env.local`의 `NEXT_PUBLIC_USE_MOCK` 값 하나로 전환한다.

- **일반 환경 (`USE_MOCK=false`)** : 실제 BE(`:8080`)에 붙어 동작. 로그인·대화·이력이 모두 실 데이터. **BE 기동 + `OPENAI_KEY` 필요**.
- **mock 환경 (`USE_MOCK=true`)** : BE 없이 fe 단독 동작. `src/lib/api/mock`의 인프로세스 목이 응답. UI·플로우 개발용.

시드 계정(둘 다 공통): **`tester` / `password1234`**

---

## 1. 일반 환경 (실 BE 연동)

**환경변수** — `.env.local`

```env
NEXT_PUBLIC_USE_MOCK=false
BACKEND_API_URL=http://localhost:8080
```

**실행 커맨드**

```bash
# 1) BE 먼저 (별도 터미널, :8080)
cd ../be && OPENAI_KEY=sk-... ./gradlew :modules:applications:api-application:bootRun

# 2) FE
npm run dev
```

**설명** : 브라우저는 항상 same-origin `/api/proxy`로만 호출하고 Next 서버가 이를 `BACKEND_API_URL`로 프록시하므로 **BE에 CORS 설정이 필요 없다**. 메시지 전송(`/chat/turns`)은 실제 LLM을 호출하므로 BE에 `OPENAI_KEY`가 있어야 한다(없으면 500).

---

## 2. mock 환경

**환경변수** — `.env.local`

```env
NEXT_PUBLIC_USE_MOCK=true
# BACKEND_API_URL 불필요
```

**실행 커맨드**

```bash
npm run dev
```

**설명** : BE 없이 목 핸들러가 계약(에러 포맷·지연 포함)을 흉내 내 응답한다. `tester / password1234`로 로그인되고, 위젯·전송·이력 복원 등 UI 플로우를 그대로 확인할 수 있다.

---

## ⚠️ 모드 전환 시 주의

`NEXT_PUBLIC_USE_MOCK`은 클라이언트 번들에 **인라인**되므로, `.env.local`만 바꾸면 이미 열린 탭에는 반영되지 않는다. **dev 서버 재시작 + 브라우저 하드 리프레시(⌘⇧R)** 필요.

접속: http://localhost:3000 → `/login`

---

## 참고

- API 계약 정본: [`specs/api-handoff.md`](./specs/api-handoff.md)
- 프로젝트 현황 인덱스: [`.reporules/`](./.reporules/)
