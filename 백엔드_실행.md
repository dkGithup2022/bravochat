# How to Start — BE (로컬 실행 가이드)

브라보 챗 백엔드를 로컬에서 띄우는 방법. Spring Boot 3.2.1 / Java 21 / H2(파일 DB).

---

## 1. 실행 커맨드 & 필요 환경 변수

### 사전 조건
- **JDK 21** (설치 확인: `java -version` → `21.x`)
- 그 외 Gradle/DB 설치 불필요 — wrapper(`./gradlew`)와 H2 임베디드 사용.

### 환경 변수
| 변수 | 필수 여부 | 설명 |
|---|---|---|
| `OPENAI_KEY` | 챗 기능에 필수 | `POST /chat/turns`가 LLM을 호출. **없으면 해당 API만 500**. 로그인·최근대화 조회는 키 없이 동작 |

> 프로파일은 기본이 `local`이라 별도 지정 불필요. H2 파일 DB, 스키마/시드 자동 주입.

### 실행
```bash
# be 디렉토리에서
export OPENAI_KEY=sk-...        # 챗 기능 쓸 때만. 생략 시 로그인/조회만 확인 가능
./gradlew :modules:applications:api-application:bootRun
```

기동 성공 신호: 콘솔에 `Started Application in N seconds`.

| 접속 지점 | URL |
|---|---|
| Swagger UI (API 직접 호출) | http://localhost:8080/swagger-ui.html |
| H2 Console (DB 확인) | http://localhost:8080/h2-console |

**포트 변경**(8080 충돌 시):
```bash
./gradlew :modules:applications:api-application:bootRun --args='--server.port=8081'
```

### 빠른 동작 확인 (curl)
```bash
# 1) 로그인 → 토큰은 응답 '헤더'(Authorization)로 발급됨. -i 로 확인
curl -i -X POST http://localhost:8080/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"tester","password":"password1234"}'

# 2) 최근 대화 (OPENAI_KEY 불필요)
curl http://localhost:8080/chat/turns/recent \
  -H 'Authorization: Bearer <위에서 받은 토큰>'

# 3) 메시지 전송 (OPENAI_KEY 필요)
curl -X POST http://localhost:8080/chat/turns \
  -H 'Authorization: Bearer <토큰>' -H 'Content-Type: application/json' \
  -d '{"message":"안녕하세요"}'
```

---

## 2. 초기 값 상태 (시드 데이터)

기동 시 `schema.sql`(DDL) → `data.sql`(시드)이 실행된다. 모든 시드는 **멱등**(`WHERE NOT EXISTS` / `CREATE TABLE IF NOT EXISTS`)이라 재기동해도 중복/덮어쓰기 없음.

### 로그인 계정 — 2개 (비밀번호 모두 `password1234`)
| username | password | user_id |
|---|---|---|
| `tester` | `password1234` | 1 |
| `tester2` | `password1234` | 2 |



### 대화 시드 — `tester`(user_id=1)에만 존재
`data.sql`이 tester 앞으로 예시 Turn 4개를 심는다:
| Turn | 상태 | 용도 |
|---|---|---|
| A | COMPLETED | 일반 대화 |
| B | COMPLETED | 툴 호출 포함 (조회 결과에서 이벤트 제외 검증용) |
| C | COMPLETED | 일반 대화 |
| D | PROCESSING | 미완료 → `recent` 조회에서 제외됨 |

- `GET /chat/turns/recent`는 **완료된 Turn만**, **오래된→최신** 순으로 반환.
- `tester2`는 대화 시드 없음 → 최초 로그인 시 빈 대화 목록.

---

## 3. H2 초기화 방법 (데이터 리셋)

### 먼저: 껐다 켜도 데이터는 안 날아간다
- DB URL이 `jdbc:h2:file:./data/chatbot-bravo-be` → **파일 기반**. `be/data/` 폴더에 디스크로 저장.
- `schema.sql`은 `CREATE TABLE IF NOT EXISTS`(DROP 없음), `data.sql`은 멱등 → **재기동해도 기존 대화/세션 유지**.
- `data/`는 `.gitignore` 대상 → git에 안 올라감. 즉 **PC마다/clone마다 로컬 파일이 따로**이고, clone 직후엔 비어 있다가 첫 기동 때 시드만 채워진다.

### 완전 초기화 = `data/` 폴더 삭제
시드 상태로 되돌리려면 앱을 끄고 DB 파일을 지운 뒤 재기동:
```bash
# 1) 앱 종료 (bootRun 실행 중인 터미널에서 Ctrl+C)
# 2) H2 파일 삭제 (be 디렉토리에서)
rm -rf data/
# 3) 재기동 → schema.sql + data.sql 다시 실행되어 시드만 있는 초기 상태로 복구
./gradlew :modules:applications:api-application:bootRun
```

### 특정 데이터만 지우고 싶을 때
H2 Console(http://localhost:8080/h2-console)에서 SQL로 부분 삭제:
- 접속 정보 — JDBC URL: `jdbc:h2:file:./data/chatbot-bravo-be`, User: `sa`, Password: (빈칸)
- 예) 대화만 비우기: `DELETE FROM turn_events; DELETE FROM turns;`
- 예) 세션만 비우기(전체 강제 로그아웃): `DELETE FROM login_sessions;`

> ⚠️ `AUTO_SERVER=TRUE` 모드라 앱이 떠 있는 동안에도 H2 Console 동시 접속이 된다. 단 `data/` 폴더 삭제는 반드시 **앱을 끈 상태**에서.
