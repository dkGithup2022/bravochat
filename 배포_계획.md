# 배포 계획 — chatbot-bravo (fe+be 단일 이미지)

fe(Next.js)+be(Spring Boot)를 하나의 도커 이미지로 묶어 k8s에 배포하는 전체 계획.
작성일: 2026-07-30. CI까지 완료된 상태에서 남은 클러스터 작업을 정리한다.

---

## 0. 현재 상태 (완료된 것)

| 항목 | 상태 |
|---|---|
| 단일 이미지 Dockerfile (be bootJar + fe standalone, 548MB) | ✅ `Dockerfile`, `entrypoint.sh` |
| prod 프로파일 (외부 MySQL, `sql.init: never`) | ✅ `application-prod.yml` + mysql-connector-j |
| MySQL DDL | ✅ `be/modules/schema/src/main/resources/prod_mysql/schema.sql` |
| CI (`prod` push → 이미지 빌드/푸시) | ✅ `.github/workflows/prod.yml` — **run 성공, 이미지 푸시 확인** |
| Docker Hub 이미지 | ✅ `searchkim/bravo-chat-demo:prod` (+ `:<sha>`) |
| argocd 매니페스트 | ✅ `argocd/prod/deployment.yml`, `service.yml` |
| k8s 수동 리소스 틀 | ✅ `temp_k8s/` (gitignore — 실값 채움용) |
| 로컬 검증 | ✅ prod 프로파일 + MySQL8 컨테이너로 로그인·세션 영속까지 통과 |

- 아키텍처: 브라우저 → fe(3000, BFF 프록시) → 같은 컨테이너의 be(8080) → 외부 MySQL.
  be는 Service에 노출하지 않음. 인증은 Bearer 토큰 릴레이.
- 트리거 브랜치: **`prod`** (master 검증 후 ff-merge → push가 배포 트리거).

---

## 1. DB 준비 (prod-mysql, 1회) — 담당: 수동

```bash
kubectl -n prod-mysql get pods                              # mysql 파드 확인
kubectl -n prod-mysql exec -it <mysql-pod> -- mysql -uroot -p
```
```sql
CREATE DATABASE chatbot_bravo_prod CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'chatbot_bravo'@'%' IDENTIFIED BY 'bravo';
GRANT ALL PRIVILEGES ON chatbot_bravo_prod.* TO 'chatbot_bravo'@'%';
FLUSH PRIVILEGES;
```
```bash
# 스키마 (로컬에서, MySQL8 검증 완료된 파일)
kubectl -n prod-mysql exec -i <mysql-pod> -- \
  mysql -uchatbot_bravo -pbravo chatbot_bravo_prod < temp_k8s/schema-mysql.sql

# 확인: 테이블 5개 (login_sessions, schedules, turn_events, turns, users)
kubectl -n prod-mysql exec -it <mysql-pod> -- \
  mysql -uchatbot_bravo -pbravo chatbot_bravo_prod -e "SHOW TABLES;"
```
```sql
-- 로그인 유저 등록 (해시 생성: htpasswd -bnBC 10 "" '<비번>' | tr -d ':\n')
INSERT INTO users (username, password_hash, is_deleted, created_at, updated_at)
VALUES ('<username>', '<bcrypt-hash>', FALSE, UTC_TIMESTAMP(6), UTC_TIMESTAMP(6));
```

## 2. k8s 수동 리소스 (argocd 등록 전 필수) — 담당: 수동

시크릿이 없으면 파드가 ImagePullBackOff / CrashLoop 되므로 argocd보다 먼저.

```bash
kubectl apply -f temp_k8s/namespace.yml

# 이미지 pull 시크릿 — 기존 네임스페이스에서 복제가 가장 빠름
kubectl get secret dockerhubsecret -n breakin-prod-api -o yaml \
  | sed 's/namespace: breakin-prod-api/namespace: chatbot-bravo-prod/' \
  | kubectl apply -f -

# 앱 시크릿 — temp_k8s/prod-env-secret.yml 값 채운 뒤
#   OPENAI_KEY: sk-...
#   SPRING_DATASOURCE_URL: jdbc:mysql://prod-mysql-service.prod-mysql:3306/chatbot_bravo_prod
#   SPRING_DATASOURCE_USERNAME: chatbot_bravo
#   SPRING_DATASOURCE_PASSWORD: bravo
kubectl apply -f temp_k8s/prod-env-secret.yml
```

## 3. argocd Application 등록 — 담당: 수동 (파일 준비됨)

```bash
kubectl apply -f temp_k8s/argocd-app.yml
```
- source: 공개 리포 `bravochat`의 `prod` 브랜치 `argocd/prod/` → git 크리덴셜 불필요
- destination: `chatbot-bravo-prod`
- 관리 대상: deployment(시크릿 env 주입, `/healthz` 프로브, 요청 0.5CPU/1Gi·상한 1.5CPU/2Gi) + service(ClusterIP 3000)

## 4. 검증

```bash
kubectl -n chatbot-bravo-prod get pods                     # Running, READY 1/1
kubectl -n chatbot-bravo-prod logs deploy/chatbot-bravo | grep -E "profile|Started"
# → "prod" 프로파일 + Started Application

kubectl -n chatbot-bravo-prod port-forward svc/chatbot-bravo 3000:3000
curl localhost:3000/healthz                                # → ok
curl -s -X POST localhost:3000/api/proxy -H 'Content-Type: application/json' \
  -d '{"method":"POST","path":"/auth/login","body":{"username":"<username>","password":"<비번>"}}'
# → {"data":null,"status":204,"authorization":"Bearer ..."}
```

## 5. 외부 노출 (ingress + DNS)

```bash
# temp_k8s/ingress.yml 의 <YOUR_DOMAIN> 채운 뒤
kubectl apply -f temp_k8s/ingress.yml
# DNS: 도메인 → 클러스터 LB (기존 aicasebook.dev와 동일 방식)
```

---

## 운영 루프 (배포 이후)

```
master에서 작업/검증 → git checkout prod && git merge master --ff-only && git push
→ CI가 :prod 태그 덮어씀
→ argocd는 매니페스트 변경이 없으면 자동 재배포하지 않음
→ kubectl -n chatbot-bravo-prod rollout restart deploy/chatbot-bravo
   (imagePullPolicy: Always → 최신 이미지 pull)
```

## 유의사항 / 추후 개선

- **GitHub 시크릿 이름**: 워크플로우는 `DOCKERHUB_USERNAME` / `DOCKERHUB_PASSWORD` 참조.
  비밀번호 대신 Docker Hub Access Token으로 교체 권장 (유출 시 토큰만 폐기 가능).
- **OPENAI_KEY 필수**: 비어 있으면 be가 기동 자체를 실패(CrashLoop). 시크릿 등록 확인 필수.
- **스키마 변경 시**: `local_h2/schema.sql`에 도메인이 추가되면 `prod_mysql/schema.sql`에도
  반영하고 운영 DB에 수동 적용 (자동 마이그레이션 없음).
- **데모 계정**: `tester/password1234`는 로컬 H2 전용. 운영 유저는 DB에만 존재.
- **sha 태그 배포**(개선): rollout restart 대신 argocd 매니페스트에 `:<sha>`를 박는
  이미지 업데이트 자동화로 발전 가능 — 롤백도 태그 교체로 가능해짐.
- **replicas는 1 고정**: 세션이 DB라 스케일아웃 자체는 가능하지만, 현 구성 검증 범위는 1.
